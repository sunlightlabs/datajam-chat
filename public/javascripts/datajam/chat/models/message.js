(function(define, require){
  define(['chat/common'], function(){
    var $ = jQuery
      , App = Datajam.Chat
      ;

      App.Models.Message = Backbone.Model.extend({

      });

  });
})(define, require);
